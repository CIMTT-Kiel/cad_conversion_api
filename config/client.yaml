# CAD API Client Configuration

host: "149.222.200.106"  # Server-IP oder Hostname

# ports default
ports:
  converter: 8001
  embedding: 8003
  analyser: 8002
  rendering: 8004

# Option 2: Individuelle URLs falls Services auf verschiedenen Servern laufen
urls:
  converter: null 
  embedding: null  
  analyser: null   
  rendering: null  

# Timeout für Requests in Sekunden
timeout: 300

# Logging-Konfiguration
logging:
  level: "DEBUG"  
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
